/**
 * Application-wide constants, error codes, roles, and enums
 */

const HTTP_STATUS = {
  OK: 200,
  CREATED: 201,
  BAD_REQUEST: 400,
  UNAUTHORIZED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  CONFLICT: 409,
  UNPROCESSABLE: 422,
  RATE_LIMIT: 429,
  INTERNAL_ERROR: 500,
  SERVICE_UNAVAILABLE: 503
};

const ROLES = {
  ADMIN: 'admin',
  ANALYST: 'analyst',
  VIEWER: 'viewer'
};

const PERMISSIONS = {
  [ROLES.ADMIN]: ['users:*', 'records:*', 'dashboard:*'],
  [ROLES.ANALYST]: ['records:read', 'dashboard:read'],
  [ROLES.VIEWER]: ['dashboard:read']
};

const ERROR_CODES = {
  VALIDATION_ERROR: 'VALIDATION_ERROR',
  AUTH_FAILED: 'AUTH_FAILED',
  TOKEN_EXPIRED: 'TOKEN_EXPIRED',
  TOKEN_INVALID: 'TOKEN_INVALID',
  UNAUTHORIZED: 'UNAUTHORIZED',
  FORBIDDEN: 'FORBIDDEN',
  NOT_FOUND: 'NOT_FOUND',
  CONFLICT: 'CONFLICT',
  INTERNAL_ERROR: 'INTERNAL_ERROR',
  RATE_LIMIT_EXCEEDED: 'RATE_LIMIT_EXCEEDED',
  BOOTSTRAP_DISABLED: 'BOOTSTRAP_DISABLED',
  INVALID_SETUP_KEY: 'INVALID_SETUP_KEY',
  USER_INACTIVE: 'USER_INACTIVE',
  TOKEN_ROTATION_VIOLATION: 'TOKEN_ROTATION_VIOLATION',
  INVALID_CREDENTIALS: 'INVALID_CREDENTIALS'
};

const RECORD_TYPES = {
  INCOME: 'income',
  EXPENSE: 'expense'
};

const PAGINATION = {
  DEFAULT_LIMIT: 20,
  MAX_LIMIT: 100,
  DEFAULT_OFFSET: 0,
  CURSOR_ENCODING: 'base64'
};

const CACHE_TTL = {
  DASHBOARD_SUMMARY: 5 * 60, // 5 minutes
  USER_PROFILE: 10 * 60,
  RECORD_LIST: 2 * 60
};

const LOG_CONTEXT = {
  REQUEST: 'REQUEST',
  AUTH: 'AUTH',
  DATABASE: 'DATABASE',
  VALIDATION: 'VALIDATION',
  ERROR: 'ERROR'
};

const TOKEN_TYPES = {
  ACCESS: 'access',
  REFRESH: 'refresh'
};

module.exports = {
  HTTP_STATUS,
  ROLES,
  PERMISSIONS,
  ERROR_CODES,
  RECORD_TYPES,
  PAGINATION,
  CACHE_TTL,
  LOG_CONTEXT,
  TOKEN_TYPES
};